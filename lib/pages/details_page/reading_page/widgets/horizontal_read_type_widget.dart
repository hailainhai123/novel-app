import 'package:flutter/material.dart';

class HorizontalReadTypeWidget extends StatelessWidget {
  const HorizontalReadTypeWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
